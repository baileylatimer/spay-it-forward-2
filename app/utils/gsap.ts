import { gsap } from 'gsap';

// Register any GSAP plugins here if needed
// import { ScrollTrigger } from 'gsap/ScrollTrigger';
// gsap.registerPlugin(ScrollTrigger);

// Export gsap instance for use in components
export { gsap };
